import React from "react";
import { SafeAreaView, Text } from "react-native";

export default props => {
    return(
        <SafeAreaView>
            <Text>Splash2</Text>
        </SafeAreaView>
    )
}